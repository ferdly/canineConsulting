// let formattedThis = formattedAddressString(paramObject.new_updatedElement.address);
                // formattedAddressString(paramObject.new_updatedElement)
                //     .then( response => {
                //         console.log('response: ' + response);
                //         paramObject.new_updatedElement.address.city = 'DDDuluth';
                //         $w('#promiseKeeper').value = response;
                //         console.log('[~LINE 327]promiseKeeper: ' + $w('#promiseKeeper').value);
                        // paramObject.new_updatedElement.address.formatted = $w('#promiseKeeper').value;
                        // paramObject.new_updatedElement.address.formatted = response;
                    } );
                    // .then( (json) => {
                    //     console.log('json: ' + json);
                    //     return json;
                    // } );
                    // .then( result => {
                    //     console.log(result);
                    // });
                
                // console.log('[~LINE 327]formattedThis: ' + $w('#promiseKeeper').value);
                // paramObject.new_updatedElement.address.formatted = $w('#promiseKeeper').value;
                // paramObject.new_updatedElement.address.formatted = setTimeout(function(){
                //                                                             return $w('#promiseKeeper').value;
                //